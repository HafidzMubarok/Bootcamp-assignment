<h4 class="text-light">Contact Detail</h4>

<div class="container d-flex flex-column p-5 mt-5">
    <h2 class="text-light"><%= contact.name %></h2>
    <div class="pt-3 text-light">
        <span>Name: </span> <span class="fw-bold ps-4 text-end"><%= contact.name  %></span>
        <br>
        <span>Email: </span> <span class="fw-bold ps-4"><%= contact.email  %></span>
        <br>
        <span>Phone Number: </span> <span class="fw-bold ps-4"><%= contact.mobile  %></span>
    </div>
    <div class="pt-3">
        <a href="/contact" class="btn btn-secondary"><i class="bi bi-chevron-left pe-2"></i>Back</a>
    </div>
</div>